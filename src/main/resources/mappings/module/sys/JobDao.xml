<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="k0n9.module.sys.dao.JobDao">
    
	<sql id="JobColumns">
		a.id,
	    a.name,
		a.parent_id,
		a.parent_ids,
		a.icon,
		a.weight,
		a.is_show,
		(select decode(count(1),0,0,1) from SYS_JOB where PARENT_ID =a.id) hasChildren
	</sql>
	
	<!--<sql id="JobJoins">-->
		<!--LEFT JOIN sys_office p ON p.id = a.parent_id-->
    <!--</sql>-->
    
	<select id="get" resultType="Job">
		SELECT
			<include refid="JobColumns"/>
		FROM sys_job a
		<!--<include refid="JobJoins"/>-->
		WHERE a.id = #{id}
	</select>
	
	<select id="findByList" resultType="Job">
		SELECT
			<include refid="JobColumns"/>
		FROM sys_job a
		<!--<include refid="JobJoins"/>-->
		ORDER BY a.id
	</select>
	
	<select id="findByPage" resultType="Job">
		SELECT
			<include refid="JobColumns"/>
		FROM sys_job a
		<!--<include refid="JobJoins"/>-->
        <where>
            <if test="sqlMap != null and sqlMap.dsf != null">
                ${sqlMap.dsf}
            </if>
        </where>
		ORDER BY a.id
	</select>

	<select id="findByIds" resultType="Job">
        SELECT
        <include refid="JobColumns"/>
        FROM sys_job a
        <!--<include refid="JobJoins"/>-->
        <where>
          a.id in
            <foreach collection="array" index="index" item="item" open="(" separator="," close=")">
              #{item}
            </foreach>
        </where>
        ORDER BY a.id
    </select>

	<select id="findByParentIdsLike" resultType="Job">
		SELECT
			a.id,
			a.parent_id AS "parentId",
			a.parent_ids
		FROM sys_job a
		WHERE a.parent_ids LIKE #{parentIds}
		ORDER BY a.id
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO sys_job(
			parent_id,
			parent_ids, 
			name,
			weight,
			is_show
		) VALUES (
			#{parentId},
			#{parentIds}, 
			#{name},
			#{weight},
			#{isShow}
		)
	</insert>
	
	<update id="update">
		UPDATE sys_job SET 
			parent_id = #{parentId},
			parent_ids = #{parentIds}, 
			name = #{name},
			weight = #{weight},
			is_show = #{isShow}
		WHERE id = #{id}
	</update>
	
	<update id="updateParentIds">
		UPDATE sys_job SET 
			parent_id = #{parentId},
			parent_ids = #{parentIds}
		WHERE id = #{id}
	</update>

    <delete id="delete">
        DELETE FROM sys_job WHERE ID = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_job WHERE id IN
        <foreach collection="array" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
	
</mapper>