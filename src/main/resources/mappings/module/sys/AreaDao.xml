<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="k0n9.module.sys.dao.AreaDao">
    
	<sql id="areaColumns">
		a.id,
		a.parent_id,
		a.parent_ids,
		a.code,
		a.name,
		a.weight,
		a.type,
		a.icon,
		a.remarks
	</sql>
	
	<!--<sql id="areaJoins">-->
		<!--LEFT JOIN sys_office p ON p.id = a.parent_id-->
    <!--</sql>-->
    
	<select id="get" resultType="Area">
		SELECT
			<include refid="areaColumns"/>
		FROM sys_area a
		<!--<include refid="areaJoins"/>-->
		WHERE a.id = #{id}
	</select>
	
	<select id="findByList" resultType="Area">
		SELECT
			<include refid="areaColumns"/>
		FROM sys_area a
		<!--<include refid="areaJoins"/>-->
		ORDER BY a.code
	</select>
	
	<select id="findByPage" resultType="Area">
		SELECT
			<include refid="areaColumns"/>
		FROM sys_area a
		<!--<include refid="areaJoins"/>-->
        <where>
            <if test="sqlMap != null and sqlMap._USER != null">
                ${sqlMap._USER.remarks}
            </if>
        </where>
		ORDER BY a.code
	</select>

	<select id="findByIds" resultType="Area">
        SELECT
        <include refid="areaColumns"/>
        FROM sys_area a
        <!--<include refid="areaJoins"/>-->
        <where>
          a.id in
            <foreach collection="array" index="index" item="item" open="(" separator="," close=")">
              #{item}
            </foreach>
        </where>
        ORDER BY a.code
    </select>

	<select id="findByParentIdsLike" resultType="Area">
		SELECT
			a.id,
			a.parent_id AS "parentId",
			a.parent_ids
		FROM sys_area a
		WHERE a.parent_ids LIKE #{parentIds}
		ORDER BY a.code
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO sys_area(
			parent_id,
			parent_ids, 
			code, 
			name, 
			weight,
			type,
			remarks
		) VALUES (
			#{parentId},
			#{parentIds}, 
			#{code}, 
			#{name}, 
			#{weight},
			#{type}, 
			#{remarks}
		)
	</insert>
	
	<update id="update">
		UPDATE sys_area SET 
			parent_id = #{parentId},
			parent_ids = #{parentIds}, 
			code = #{code}, 
			name = #{name}, 
			weight = #{weight},
			type = #{type},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="updateParentIds">
		UPDATE sys_area SET 
			parent_id = #{parentId},
			parent_ids = #{parentIds}
		WHERE id = #{id}
	</update>

    <delete id="delete">
        DELETE FROM SYS_AREA WHERE ID = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM sys_area WHERE id IN
        <foreach collection="array" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
	
</mapper>